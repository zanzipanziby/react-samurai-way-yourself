import React from "react";

const Header = () => {
    return (
        <div className = "main-menu">
            <a href="#">Home</a>
            <a href="#">New Feed</a>
            <a href="#">Message</a>
            <a href="#">Home</a>
        </div>
    )
}

export default Header;